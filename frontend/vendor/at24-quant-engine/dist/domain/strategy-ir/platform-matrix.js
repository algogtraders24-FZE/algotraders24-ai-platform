import { computeCanonicalHash } from "../../runtime/determinism.js";
export const PLATFORM_SEMANTIC_MATRIX = [
    {
        platform: "MT4_MQL4",
        accountMode: "Hedging only (flat ticket model — no netting mode exists)",
        orderModel: "Flat ticket (OrderSend/OrderModify/OrderClose), no Order/Deal/Position separation",
        positionModel: "Each ticket is its own position; multiple same-symbol tickets coexist independently",
        barTiming: "OnTick (every tick by default); OnBarUpdate-equivalent requires manual new-bar detection",
        mtf: "iMA/iRSI/etc. accept a timeframe parameter directly; no explicit availability-boundary concept documented",
        repainting: "Indicator buffers can repaint on the still-forming current bar (shift=0) unless the strategy explicitly reads shift=1+",
        slTp: "SL/TP are per-order absolute price fields on OrderSend, not separate SL/TP orders",
        trailing: "Native trailing stop exists but is a TERMINAL/broker-side feature, not backtestable strategy logic in the same way",
        partialClose: "OrderClose supports partial-lot closes directly",
        sessions: "No native session-hours construct; strategies check TimeCurrent()/DayOfWeek() manually",
        timezone: "Server time only (broker-defined), no explicit strategy/exchange/data distinction",
        fees: "Not modeled by MQL4 itself — broker-side commission, invisible to strategy code",
        spread: "MarketInfo(SYMBOL_SPREAD) readable but not strategy-controlled",
        slippage: "Slippage parameter on OrderSend (points), broker-enforced",
        intrabar: "No native bar-magnifier concept; strategies relying on tick data use OnTick directly",
        historicalRealtime: "Undisclosed/unpublished tick-simulation algorithm in the Strategy Tester ('predefined wave templates') — AVOID per Q0.4_PLATFORM_DECISIONS.md",
        pendingOrderFill: {
            limitTouchBehavior: "UNKNOWN",
            limitPenetrationBehavior: "Not documented at the platform level with enough precision to classify — broker/dealer/build-dependent in practice; never assumed",
            stopTriggerBehavior: "UNKNOWN",
            gapThroughBehavior: "UNRESOLVED",
            triggerFillRelationship: "Not independently confirmed; MQL4's own docs do not commit to a precise touch-vs-penetration rule for pending-order fills",
            historicalExecutionModel: "PLATFORM_SPECIFIC_SYNTHETIC",
            bidAskRequirement: "REQUIRED",
            intrabarRequirement: "No native bar-magnifier; the Strategy Tester's own undisclosed synthetic tick generator is the only intrabar source, and its algorithm is unpublished (AVOID per Q0.4_PLATFORM_DECISIONS.md)",
        },
    },
    {
        platform: "MT5_MQL5",
        accountMode: "ENUM_ACCOUNT_MARGIN_MODE — explicit netting OR hedging, account-level setting",
        orderModel: "Order -> Deal -> Position, three-stage separation (CTrade class, PositionSelect)",
        positionModel: "One net position per symbol under netting mode; independent positions under hedging mode",
        barTiming: "OnTick by default; OnCalculate exists for indicators with configurable calculation mode",
        mtf: "Indicator handles (iMA, etc.) bound to an explicit timeframe parameter at creation",
        repainting: "Same current-bar risk as MQL4 unless the strategy reads a confirmed (shift>=1) buffer value",
        slTp: "SL/TP fields on the trade request structure (MqlTradeRequest), applied per position or per pending order",
        trailing: "Native trailing stop is broker/terminal-side, same caveat as MT4",
        partialClose: "PositionClosePartial supported directly",
        sessions: "No native session-hours construct; manual TimeCurrent() checks, same as MT4",
        timezone: "Server time only, same as MT4 — no explicit multi-zone distinction",
        fees: "Not modeled by MQL5 itself; broker-side",
        spread: "SymbolInfoInteger(SYMBOL_SPREAD) readable",
        slippage: "Deviation parameter on trade requests (points)",
        intrabar: "Published 3-5-3-point tick-generation algorithm when real ticks unavailable (documented, unlike MT4) — ADOPT per Q0.4_PLATFORM_DECISIONS.md",
        historicalRealtime: "Same undocumented-vs-documented tick generation distinction as above; MT5's is the disclosed one",
        pendingOrderFill: {
            limitTouchBehavior: "UNKNOWN",
            limitPenetrationBehavior: "Not documented at the platform level with enough precision to classify",
            stopTriggerBehavior: "UNKNOWN",
            gapThroughBehavior: "UNRESOLVED",
            triggerFillRelationship: "Not independently confirmed at the touch-vs-penetration level",
            historicalExecutionModel: "PLATFORM_SPECIFIC_SYNTHETIC",
            bidAskRequirement: "REQUIRED",
            intrabarRequirement: "The published 3-5-3-point synthetic tick-generation algorithm (documented, unlike MT4) is the intrabar source when real ticks are unavailable — a disclosed, but still SYNTHETIC (not real-tick), reconstruction",
        },
    },
    {
        platform: "TRADINGVIEW_PINE",
        accountMode: "No account-mode concept — strategy.* functions manage one net position per strategy() call by default (pyramiding parameter controls same-direction accumulation)",
        orderModel: "strategy.entry()/strategy.exit()/strategy.order() — declarative, engine resolves fills",
        positionModel: "Single net position per strategy instance unless pyramiding is explicitly enabled",
        barTiming: "calc_on_every_tick vs calc_on_bar_close strategy() parameter — directly the Q0.4.6-researched divergence",
        mtf: "request.security() with barmerge.lookahead_on/lookahead_off — the researched HTF-leakage footgun (Q0.4_LOOKAHEAD_REPAINTING.md)",
        repainting: "Well-documented repainting risk on unconfirmed request.security() reads and on realtime (unclosed) bar values",
        slTp: "strategy.exit() with loss/profit parameters, measured from strategy.position_avg_price by default",
        trailing: "strategy.exit() trail_points/trail_offset parameters, native",
        partialClose: "strategy.close()/strategy.exit() with qty_percent",
        sessions: "session.ismarket / input.session-style constructs; timezone via the chart's own settings unless overridden",
        timezone: "Chart timezone by default; time() function accepts an explicit timezone string argument to override",
        fees: "commission_value/commission_type strategy() parameters, native",
        spread: "Not modeled at the strategy() level; broker-emulator dependent",
        slippage: "slippage strategy() parameter (in ticks)",
        intrabar: "Pine's Bar Magnifier feature (lower-timeframe intrabar order resolution), with a documented 200,000-bar cap causing SILENT mid-run fidelity degradation — AVOID that silence per Q0.4_PLATFORM_DECISIONS.md",
        historicalRealtime: "Explicitly documented divergence between historical and realtime bar evaluation — the platform most transparent about this asymmetry existing at all",
        pendingOrderFill: {
            limitTouchBehavior: "TOUCH_SUFFICIENT",
            limitPenetrationBehavior: "By default, ANY price within the bar's OHLC range counts as reaching the level — mere touch, not trade-through. A stricter opt-in tick-count tolerance (`backtest_fill_limits_assumption`) exists but is NOT the default (docs/Q0.4_PINE_SEMANTICS.md).",
            stopTriggerBehavior: "TOUCH_TRIGGER",
            gapThroughBehavior: "FILL_AT_TRIGGER",
            triggerFillRelationship: "Pine's lenient default effectively claims the fill happened AT the requested price whenever it falls within the bar's range, even across what would otherwise be a gap — a real, documented divergence from a strict trigger-vs-fill separation. AT24 deliberately does NOT replicate this default (docs/Q0.4_PINE_SEMANTICS.md's adopted-standard note).",
            historicalExecutionModel: "BAR",
            bidAskRequirement: "NOT_REQUIRED",
            intrabarRequirement: "BAR by default (4-point O/H/L/C inference); Bar Magnifier upgrades this to LOWER_TIMEFRAME_RECONSTRUCTION when enabled, subject to the documented 200,000-bar-per-script cap (plan-gated on TradingView; the cap itself is the risk AT24 avoids replicating silently)",
        },
    },
    {
        platform: "NINJATRADER_NINJASCRIPT",
        accountMode: "Netting by default per instrument within a strategy instance; ATM strategies can layer",
        orderModel: "EnterLong/EnterShort/ExitLong/ExitShort + native OCO/bracket order support",
        positionModel: "One position per instrument per strategy by default; BarsInProgress supports multi-instrument/multi-series",
        barTiming: "OnBarUpdate with a Calculate mode (OnBarClose/OnEachTick/OnPriceChange) — explicit, native, best-documented calculation-timing control among all researched platforms",
        mtf: "Multi-series via AddDataSeries + BarsInProgress index checks",
        repainting: "Calculate.OnEachTick strategies can see repainting-equivalent behavior on the still-forming bar, same general risk as Pine/MQL",
        slTp: "SetStopLoss/SetProfitTarget, native, separate from EnterLong/EnterShort calls",
        trailing: "Native trailing-stop support via SetStopLoss with a TrailingUnit type",
        partialClose: "Supported via explicit partial ExitLong/ExitShort quantity",
        sessions: "TradingHours template construct, native — but a documented Trading-Hours-DISPLAY-setting-vs-stored-value footgun (timezone display not affecting already-stored values) — AVOID that pattern",
        timezone: "Instrument's exchange timezone by default, explicit override supported",
        fees: "Commission set per-instrument in the simulation account, native",
        spread: "Not modeled at the strategy level directly; simulated fill engine dependent",
        slippage: "Slippage ticks parameter on the simulated fill engine",
        intrabar: "Tick Replay (calculation timing) and High-Order-Fill-Resolution (fill price) are explicitly SEPARATE settings per NinjaTrader's own docs — ADOPT that separation of concerns",
        historicalRealtime: "Zero-by-default slippage/commission cited as a 30-50% profit-factor overstatement risk in NinjaTrader research — AVOID that default",
        pendingOrderFill: {
            limitTouchBehavior: "UNKNOWN",
            limitPenetrationBehavior: "Not documented at the platform level with enough precision to classify — likely configurable via High-Order-Fill-Resolution, but the exact touch/penetration rule was not independently confirmed",
            stopTriggerBehavior: "UNKNOWN",
            gapThroughBehavior: "UNRESOLVED",
            triggerFillRelationship: "Not independently confirmed",
            historicalExecutionModel: "BAR",
            bidAskRequirement: "UNKNOWN",
            intrabarRequirement: "Tick Replay (WHEN calculation happens) and High-Order-Fill-Resolution (WHAT price a fill uses) are documented as separate, independently-configurable settings — a real, cited architectural separation worth adopting, but the DEFAULT fidelity absent either setting was not independently confirmed",
        },
    },
    {
        platform: "CTRADER_CBOT",
        accountMode: "Both netting and hedging natively supported, selectable",
        orderModel: "ExecuteMarketOrder/PlaceLimitOrder/PlaceStopOrder + Positions/PendingOrders collections",
        positionModel: "Position/PendingOrder split, similar spirit to MT5's Order/Deal/Position separation",
        barTiming: "OnBar (bar-close) vs OnTick (every tick), explicit dual entry points",
        mtf: "MarketData.GetBars(timeframe) for any timeframe, no documented lookahead guard equivalent to Pine's",
        repainting: "Same general current-bar risk as other tick-driven platforms absent explicit confirmed-bar reads",
        slTp: "StopLossPips/TakeProfitPips on order-execution calls, or ModifyPosition after the fact",
        trailing: "Native trailing stop via Position.ModifyTrailingStop or built-in trailing parameters",
        partialClose: "ClosePosition supports a volume parameter for partial closes",
        sessions: "No native session-hours construct; manual Server.Time checks",
        timezone: "Server.Time is broker/server time; no explicit multi-zone distinction documented",
        fees: "Symbol.Commission readable; account-configured",
        spread: "Bid/ask-native data model (cTrader is the top-fidelity-tier precedent per Q0.4_PLATFORM_DECISIONS.md) — spread is a first-class, always-available value, not inferred",
        slippage: "MarketOrder execution accepts a slippage tolerance in pips",
        intrabar: "Bid/ask-native, resolves many spread-dependent fill questions trade-tick-only data cannot",
        historicalRealtime: "One execution engine (cAlgo.API) shared by C# and Python front-ends — ADOPT as a principle: one canonical engine, multiple language front-ends",
        pendingOrderFill: {
            limitTouchBehavior: "UNKNOWN",
            limitPenetrationBehavior: "Not documented at the platform level with enough precision to classify",
            stopTriggerBehavior: "UNKNOWN",
            gapThroughBehavior: "UNRESOLVED",
            triggerFillRelationship: "Not independently confirmed at the touch-vs-penetration level, though the bid/ask-native data model plausibly resolves more of this precisely than OHLC-only platforms can",
            historicalExecutionModel: "TICK",
            bidAskRequirement: "REQUIRED",
            intrabarRequirement: "Bid/ask-native tick data is the platform's own default execution model — a genuinely finer-grained intrabar source than any of the other researched platforms, which is exactly why AT24's own BID/ASK PriceReference kinds exist as named-but-UNSUPPORTED (Q0.11.3): cTrader's own model assumes data AT24's OHLCV-only simulation does not have",
        },
    },
    {
        platform: "AT24_NATIVE",
        accountMode: "NETTING only implemented (Q0.5); HEDGING type reserved, not implemented (docs/Q0.5_POSITION_ACCOUNT.md)",
        orderModel: "SimulationOrder full lifecycle state machine (Q0.5, docs/Q0.5_ORDER_ENGINE.md)",
        positionModel: "One net position per instrument (Q0.5)",
        barTiming: "ON_BAR_CLOSE only (Q0.5.29); ON_EVERY_TICK/ON_ORDER_FILL reserved, not implemented",
        mtf: "D2/D3 execution-fidelity child-bar walking (Q0.6); no genuine dual-timeframe STRATEGY calculation yet (docs/Q0.6_MTF_SAFETY.md)",
        repainting: "Structurally impossible for AT24's own indicators (incremental step functions, Q0.2.5) and TimeFrontier-gated bars",
        slTp: "RiskSpecification.stopLoss/takeProfit (Q0.2/Q0.3), resolved via BarFillModel (Q0.5) — conservative ambiguity resolution",
        trailing: "RiskSpecification.trailingStop (Q0.2/Q0.3)",
        partialClose: "RiskSpecification.partialClose (Q0.2/Q0.3), evaluated by evaluateRisk() (Q0.3)",
        sessions: "RiskSpecification.sessionHours with an explicit IANA timezone field (Q0.2)",
        timezone: "Never the host machine's local timezone; explicit IANA names throughout (Q0.3)",
        fees: "ExecutionSpecification (Q0.2), FeeModel (Q0.5) — costsExplicitlyZero required to declare intentional zero cost",
        spread: "SpreadModel (Q0.5), Zero/Fixed implementations shipped",
        slippage: "SlippageModel (Q0.5), Zero implementation shipped",
        intrabar: "Conservative bar-fill model (Q0.5), extended to D2/D3 child-bar walking (Q0.6) — never silently favorable",
        historicalRealtime: "No realtime execution exists yet (simulation only) — the distinction does not yet apply, reserved for a future live-execution sprint",
        pendingOrderFill: {
            limitTouchBehavior: "PENETRATION_REQUIRED",
            limitPenetrationBehavior: "Any nonzero amount strictly past the limit level (the bar's low/high must go BEYOND it, not merely equal it) — `resolveLimitFill()` (Q0.5, unmodified): BUY requires `low < limit`, mere touch (`low === limit`) is NOT sufficient. This is Q0.4's own adopted standard, deliberately stricter than Pine's lenient default (docs/Q0.4_PINE_SEMANTICS.md).",
            stopTriggerBehavior: "TOUCH_TRIGGER",
            gapThroughBehavior: "FILL_AT_OPEN",
            triggerFillRelationship: "Never assumed equal for a gapped STOP — `resolveStopFill()` fills at the bar's own open, which may be materially worse than the stop level, never fabricated as the stop price itself (Q0.11's own explicit rule). For LIMIT, always at-or-better than requested: either the open (if already favorable) or exactly the limit price on strict trade-through.",
            historicalExecutionModel: "LOWER_TIMEFRAME_RECONSTRUCTION",
            bidAskRequirement: "NOT_REQUIRED",
            intrabarRequirement: "D1_OHLC (Q0.5) uses whole-bar OHLC only (BAR-equivalent, conservative); D2_LOWER_TIMEFRAME/D3_M1 (Q0.6) reconstruct REAL child-bar sequences via BarDetailProvider — never synthesized (Q0.6.38) — and fidelity is recorded as a PER-RESULT field, never an assumed run-wide constant (a lesson drawn directly from Pine's own 200,000-bar-cap silent-degradation risk, avoided by design).",
        },
    },
];
export function getPlatformProfile(platform) {
    return PLATFORM_SEMANTIC_MATRIX.find((p) => p.platform === platform);
}
/**
 * Q0.12.13 — bumped whenever ANY execution-semantic rule in
 * `PLATFORM_SEMANTIC_MATRIX` changes (a field's value, a new field, or a
 * reclassification of an existing platform) — never bumped for pure
 * prose/wording edits that don't change a documented semantic claim.
 */
export const PLATFORM_SEMANTIC_PROFILE_VERSION = "1.0.0";
/**
 * Q0.12.13/38 — a single canonical hash over the ENTIRE semantic matrix
 * plus its version, so that changing any platform's execution semantics
 * (including adding a new platform) changes this hash deterministically.
 * Reused wherever a result's provenance needs to prove WHICH semantic
 * understanding of the source platform was in effect (Q0.12.41) —
 * never a second, divergent hash of the same concept.
 */
export function computeSemanticProfileHash() {
    return computeCanonicalHash({ version: PLATFORM_SEMANTIC_PROFILE_VERSION, matrix: PLATFORM_SEMANTIC_MATRIX });
}
