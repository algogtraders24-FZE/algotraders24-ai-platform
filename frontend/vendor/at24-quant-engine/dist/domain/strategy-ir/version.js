/**
 * Q0.7.35 — the IR SCHEMA version (distinct from a strategy's own
 * `strategyVersion`, and from a source platform's `sourceVersion`). A
 * change to how the IR interprets an existing semantic construct must
 * bump this — this package's own runtime never bumps it silently.
 */
export const STRATEGY_IR_VERSION = "0.1.0";
