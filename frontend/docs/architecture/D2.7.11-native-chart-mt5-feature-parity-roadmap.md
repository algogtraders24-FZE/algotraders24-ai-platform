# D2.7.11 - Native Chart MT5 Feature-Parity Roadmap

## Why this exists

The user's explicit, long-term ask (this session): make the Native Chart not just LOOK like their live MT5 terminal (D2.7.x's own "mt5" canvas theme - hollow candles, black background, teal current-price line - already shipped), but actually carry the FEATURES they use in real MT5 day to day. Their own words: "sirf mai usme use honewale feature bhi implement karna chahta hu" (I also want the features it uses implemented) and "sare banao, time lelo, lekin hame ye chahiye long term goal ke liye" (build all of them, take your time, this is for a long-term goal).

This is deliberately scoped as a multi-phase program, not a single sprint - the full MT5 feature set (44 analytical objects, 38 built-in indicators, multi-chart workspaces, saved templates) is large. Each phase below ships independently, fully tested, with zero regression to what already exists - the same discipline every D2.7.x phase has followed.

**Not in scope for this roadmap:** no order execution, no BUY/SELL, no "Algo Trading" toggle, no broker connectivity beyond the existing read-only MT5 market-data bridge - this chart shows and annotates price, it never places a trade. (2026-08-21: the user's stated long-term plan is to build a paper-trading terminal on top of this chart later, so this is a scoping boundary for the current roadmap, not a permanent platform invariant - order execution is simply its own, not-yet-opened epic, distinct from chart feature-parity work.)

## Research basis

MetaTrader 5's real feature set (metatrader5.com/mql5.com, verified this session):
- **46 analytical objects** across 8 categories: Lines, Channels, Gann Tools, Fibonacci Tools, Elliott Tools, Shapes, Arrows, Graphical Objects.
- **38 built-in technical indicators** across 4 categories: Trend, Oscillators, Volumes, Bill Williams' tools.
- Multi-chart workspaces (multiple symbol/timeframe charts open as tabs/tiles within one terminal window).
- Per-chart saved templates (a named bundle of indicators/objects/colors, reusable across symbols) and object persistence across terminal restarts.

## Phase status

### Phase 1 - Drawing Tools foundation: SHIPPED (this session)

Trend Line, Horizontal Line, Rectangle. Object model, hit-testing (endpoint + body handles), drag-to-move/resize, 2-click placement with a live preview, Delete/Escape, sessionStorage persistence per symbol+timeframe (tab-scoped - see Phase 1b below for why this isn't durable cross-session storage yet).

Files: `lib/chart-engine/drawing/{types,geometry,store,drawing-renderer}.ts`, `components/chart-engine/DrawingToolbar.tsx`, `NativeChart.tsx` wiring. Tests: `scripts/validate-native-chart-drawing-tools.ts` (27 assertions), zero regression across the existing 698+ native-chart assertions.

### Phase 1b - Fibonacci Retracement + durable persistence: SHIPPED

- **Fibonacci Retracement** (`OBJ_FIBO`): SHIPPED (commit `7175977`). Reuses the exact same 2-click placement/drag/select pipeline Phase 1 already built (a trend-line-shaped anchor pair, plus MT5's real ratio levels: 0, 0.236, 0.382, 0.5, 0.618, 0.786, 1 drawn as horizontal sub-lines between the two anchors) - additive to the existing `DrawingObject` union, not a rework.
- **Durable persistence**: SHIPPED. The open design decision (new Prisma model vs. extending WorkspacePreference) was resolved in favor of a dedicated table - `ChartDrawingSet` (one row per `userId`+`symbol`+`timeframe`, the exact compound key the old sessionStorage entries already used, storing the whole object array as JSONB). WorkspacePreference is a flat one-row-per-user model with no per-symbol dimension at all, so it couldn't hold a per-(symbol,timeframe) array without becoming a second table in practice anyway. New: `services/chart/chart-drawing.service.ts` (validation, whole-array-replace upsert), `GET`/`PUT /api/private/chart-drawings`, `lib/chart-engine/drawing/validation.ts` (the one `isValidDrawingObject` shared by client and server). `lib/chart-engine/drawing/store.ts`'s `readDrawingObjects`/`writeDrawingObjects` kept their names/roles but became async fetch calls; writes for the same symbol+timeframe key are serialized through a per-key promise queue in store.ts so async network resolution can never land out of order (a rapid add-then-delete could otherwise resurrect the deleted object). `NativeChart.tsx`'s load/save effects gained a `drawingsLoadedKeyRef` guard (mirrors the file's existing `fittedKeyRef` pattern) so the "clear local state while the new symbol's real data loads" step can never itself be mistaken for a real edit and overwrite a symbol's saved drawings with `[]` before they've loaded. Tests: `scripts/validate-chart-drawing-persistence.ts` (11 assertions, real Postgres, synthetic tagged user, self-cleaning) + the persistence section of `scripts/validate-native-chart-drawing-tools.ts` (fetch-mocked, including a dedicated out-of-order-write race test).

### Phase 2 - Expanded indicator library: SHIPPED (this session)

Today's `DEFAULT_INDICATOR_CONFIGS` (EMA-20/50, SMA-20, Bollinger-20/2, RSI-14, MACD-12/26/9, Volume) covered 6 of MT5's 38 at the start of this phase. All 7 planned additions are now shipped, each with its own real MT5-verified default (mql5.com/metatrader5.com, never a textbook/invented value) and its own dedicated test coverage in `scripts/validate-native-chart-indicators-phase2.ts` (49 assertions):

1. **ATR-14** - own sub-panel, dynamic 0..max scale.
2. **Stochastic Oscillator 5/3/3** ("Slow Stochastic", MT5's real default - not the textbook fast-stochastic every other platform ships) - own sub-panel, %K/%D lines, real 80/20 reference lines.
3. **ADX-14** - own sub-panel, ADX/+DI/-DI three-line display, deliberately no reference line (MT5's own ADX indicator has none by default).
4. **CCI-14** (MT5's real default - not Lambert's original 20) - own sub-panel, real +-100 reference lines.
5. **Williams %R-14** - own sub-panel, real -20/-80 reference lines (the mirror of Stochastic's 80/20, on its own -100..0 range).
6. **Parabolic SAR 0.02/0.2** - price overlay, discrete dots (never a connected line - a stop-and-reverse LEVEL, not a continuous value).
7. **Ichimoku Kinko Hyo 9/26/52** (Tenkan/Kijun/Senkou) - price overlay, five lines (Tenkan-sen, Kijun-sen, Senkou Span A, Senkou Span B, Chikou Span). The architecturally distinct one of the batch: Senkou Span A/B plot `kijunPeriod` (26) candles AHEAD of the candle they're computed from, and Chikou Span plots 26 candles BEHIND - genuine time-shifted output, not just a new formula. This works with zero renderer changes because the chart's existing gapless index-scale extrapolation (`fractionalIndexForTime` in `index-scale.ts`) already positions a time beyond the real candle range correctly in both directions - a shifted `IndicatorPoint.time` (a real value, honestly computed, at a synthetic/extrapolated time) renders exactly like a real candle's time would. The cloud fill between Senkou Span A/B (a cosmetic convenience some platforms shade) was deliberately left as two plain lines rather than a filled polygon - filling would need `ctx.closePath()`/`ctx.fill()`, two canvas primitives this codebase's test-fake `CanvasRenderingContext2D` mocks don't implement, the same "don't introduce an untested canvas primitive" discipline `strokeRect`/`ctx.arc` were avoided for elsewhere in this phase.

Bill Williams' tools (Alligator, Awesome Oscillator, Fractals) remain the one deferred, lower-priority item from the original Phase 2 scope - niche relative to the 7 shipped above; not currently planned as a near-term next step.

Each indicator followed the existing `lib/chart-engine/indicators/compute.ts` pattern exactly (a pure function keyed by `IndicatorConfig.id`, added to `panel-registry.ts`'s `DEFAULT_INDICATOR_CONFIGS` and `INDICATOR_PANEL_ID`) - no new architecture needed for 6 of the 7; Ichimoku's time-shifted lines were the one genuine architectural question this phase raised, and resolved without any renderer/index-scale change (see above).

### Phase 3 - Multi-symbol tiled chart layout: SHIPPED

MT5's ability to have several symbol/timeframe charts open and genuinely SIMULTANEOUSLY VISIBLE (real tiling - a grid of independently-driven charts, not tabs where only one is ever mounted at a time). This was, as flagged, the largest architectural change in the whole roadmap - a dedicated read-only investigation (an Explore agent mapping every consumer of `WorkspaceContext.symbol`) ran before any code, confirming the actual blast radius: `symbol` is read directly by 9 components, but only the chart itself needed to become multi-instance. Every other panel (header, AI Intelligence, Assistant, Research, ribbon, favorites, the global search) deliberately keeps following ONE shared "active" symbol - duplicating those N times per pane would fight how this product actually works (deep-dive intelligence tools tied to one instrument, not a simple MT5-style Market Watch list), and there is no real MT5 analog for "AI Intelligence panel locked to symbol X while chart pane B shows symbol Y" to imitate anyway.

**Scope, deliberately narrowed and documented (not silently invented):**
- **Native-engine only.** This whole roadmap has only ever touched the Native chart - TradingView (`AdvancedChart.tsx`) is completely untouched, stays a single non-tiled instance always following `WorkspaceContext.symbol` exactly as before this phase. The provider toggle stays global; a layout selector only appears when Native is active.
- **Real grid presets (1 / 2 / 2×2), not a full drag/resize window manager.** MT5's own common tiling arrangements; freeform window management would be a separate, much larger undertaking with little added value over fixed presets.
- **`panes` always has exactly `layout` entries** (front-truncated on shrink, grown from the primary pane's own symbol on grow) - simpler and more honest than silently keeping a hidden 4-pane buffer in memory "just in case."
- **Session-scoped persistence, not a new durable DB table** - mirrors exactly how Phase 1's drawn-object persistence started sessionStorage-only before Phase 1b later earned it a real table once proven. Keeps this already-large phase contained; durable tab/layout persistence is a natural, separately-flagged future increment, not bundled in here.

**Design**: exactly one pane is "primary", bidirectionally synced with `WorkspaceContext.symbol` - switching which pane is primary (or editing the primary pane's own symbol) updates the page's shared active symbol, so the rest of the workspace follows whichever chart the user designated as driving it (the exact pre-Phase-3 single-chart behavior, now a deliberate choice among several open panes instead of the only option). Every other pane owns a fully independent symbol/timeframe/indicator set with no such sync. Changing the symbol via the global search/ribbon/favorites updates the currently-primary pane in place - it never silently spawns a new pane.

**Files**: `NativeChart.tsx`'s `symbol`/`name` became controlled props (previously read from `WorkspaceContext` directly) - the one genuinely deep change, since a page can now have several `NativeChart` instances mounted at once, each showing a different instrument, which a single shared context value could never represent. Its `hypothesisType` relay (D2.8.13, fed by `WorkspaceResearch` for whichever symbol is primary) gained a guard - `symbol === activeSymbol` - so a pane showing a DIFFERENT instrument never receives another instrument's hypothesis. New `ChartPane.tsx` (one grid cell: a compact per-pane header - independent instrument search + "set primary" control, shown only when more than one pane is visible - above one `NativeChart`). `GlobalSymbolSelector.tsx`'s real search/debounce/keyboard-nav/discovery implementation was extracted into a new generic `InstrumentSearchBox.tsx` (`value`/`onChange` props, zero `WorkspaceContext` coupling) so each pane's own search reuses the exact same real behavior instead of a second, drifting copy - `GlobalSymbolSelector` is now a thin wrapper around it. `ChartPanel.tsx` was rewritten to own `panes`/`layout`/`primaryPaneId` (previously a single flat `timeframe`/`activeIndicatorKeys` pair) and `chart-session-state.ts`'s persisted shape was restructured to match (storage key bumped to `.v2`, old shape retired).

**Drawing objects, templates, and useChartCandles/useLiveQuote needed zero changes** - they already keyed off a `symbol` parameter (never `WorkspaceContext` directly), so N simultaneously-mounted panes each showing a different instrument was already safe at the data layer.

Tests: `scripts/validate-native-chart-tiling.ts` (19 assertions - layout growth/shrink, primary-pane sync in both directions, the hypothesisType guard, the InstrumentSearchBox extraction, session persistence) + updated assertions across `validate-native-chart-workspace.ts`, `validate-native-chart-verification.ts`, `validate-native-chart-professionalization.ts`, and `validate-universal-instrument-discovery.ts` wherever they encoded the pre-Phase-3 "NativeChart reads symbol from context" / "one flat session-state shape" assumptions as literal source-text checks - each one was a real, load-bearing assertion that needed a genuine update, not a deletion.

### Phase 4 - Object/template management: SHIPPED

Save a named bundle of active indicators + drawn objects as a reusable "template" (MT5's own Template feature), list/apply/delete templates. New `ChartTemplate` table (one row per `userId`+`name`, deliberately NOT scoped by symbol/timeframe - a template is a per-user library applicable to any chart, matching real MT5 template behavior including its own real limitation: a drawn object's saved price/time coordinates are reapplied as-is to whatever symbol/timeframe the template lands on, which can look nonsensical on a very different instrument - MT5 itself has this same limitation, so this isn't a shortfall to engineer around). Saving under an existing name is a deliberate upsert (overwrite), not a rejected duplicate.

New: `services/chart/chart-template.service.ts` (validation against the one canonical `DEFAULT_INDICATOR_CONFIGS` registry + `isValidDrawingObject`, per-user 50-template limit), `GET`/`POST /api/private/chart-templates` + `DELETE /api/private/chart-templates/:id`, `lib/chart-engine/templates/store.ts` (async fetch wrappers, same never-throws contract as `drawing/store.ts`). UI: a second "Templates" dropdown in `ChartToolbar.tsx` (own dismissal state, independent of the Indicators dropdown) with a `Modal`-based (`components/ui/Modal.tsx`, not a native `prompt()`) "Save as Template…" flow in `NativeChart.tsx`. Applying a template replaces BOTH halves at once: the active indicator set (bubbles up to `ChartPanel.tsx`, which owns that state, via a new `onApplyIndicatorKeys` prop distinct from the existing per-key `onToggleIndicator`) and the current chart's drawn objects (`commitDrawingObjects`, which piggybacks on Phase 1b's existing persistence-write effect - no new plumbing needed there). Tests: `scripts/validate-chart-template-persistence.ts` (11 assertions, real Postgres, self-cleaning) + `scripts/validate-native-chart-templates.ts` (14 assertions: fetch-mocked client store + source-text wiring checks for all three touched components).

### Phase 5 - Chart Properties & Presentation: IN PROGRESS

Opened after the user shared 4 real MT5 desktop screenshots (the chart right-click context menu, and the Properties dialog's Common/Show/Colors tabs) and asked what else from those is genuinely implementable. Scoped down from the full menu to pure presentation/UX items with no order-execution surface: chart type (Bar/Candlesticks/Line), a Properties panel (grid, period separators, OHLC-readout toggle, object-descriptions toggle, color-scheme presets), Save-as-Picture (PNG export), and an Object List panel (manage drawn objects). Explicitly excluded: Depth of Market, One Click Trading, Trade Levels/History, Expert List (order-execution-adjacent, out of scope per the working agreement below) and tick-vs-real-volume toggle / Chart Shift / Docked (low value regardless of scope - real volume is meaningless for forex/CFD, Chart Shift/Docked are desktop-window concepts with no browser analog).

**Strategic context**: the user's stated long-term intent is to use this chart as the foundation for a future paper-trading terminal, competing with other platforms - so this phase is held to a genuine MT5-parity bar rather than a "good enough" one. Paper trading itself (virtual portfolio, simulated order matching, BUY/SELL UI) is a distinct, not-yet-scoped future epic - this roadmap still touches zero order-execution surface.

**Phase 5a - Chart type toggle (Candles/Bars/Line): SHIPPED, live-verified** (commit `39563c3`). `renderChart()` gained an optional `chartType` param (default `"candlestick"`, so every existing caller renders byte-for-byte unchanged); new `drawBars` (plain OHLC bar - high-low line + open/close ticks, no filled body) and `drawLine` (single polyline through each candle's close, styled in the current-price marker's accent color) share the exact same index-scale x positioning as the existing `drawCandles`. `chartType` is local `NativeChart` state (like `isLive`/`activeTool`), deliberately NOT lifted to `ChartPanel` - it's a rendering-style preference, not per-instrument data. Live-verified on production in a 2x2 tiled layout: one pane set to Bars, a different pane to Line, simultaneously and independently.

**Phase 5b - Properties panel: Grid + Period separators: SHIPPED, live-verified** (commit `ca4065a`). `renderChart()` gained `showGrid` (default true, preserves the pre-existing always-on grid) and `showPeriodSeparators` (default false, matches real MT5's own default per the user's Properties dialog screenshot). New `computePeriodSeparators` (`time-axis.ts`) finds UTC day boundaries, scoped to intraday timeframes only - a D1+ bar already IS a day, so a same-scale boundary line there would be redundant. New "Properties" toolbar button opens a `Chart Properties` Modal (reusing the existing `Modal` component, same one Save-as-Template already uses) with Grid/Period separators checkboxes. Deferred from this slice: OHLC-readout toggle (our OHLC display is a different, hover/crosshair-driven feature from MT5's always-visible latest-bar readout) and object-descriptions toggle (nothing to toggle until the Object List panel gives drawn objects real labels). Live-verified on production: Grid off genuinely removes gridlines (confirmed via zoomed screenshot), Period separators on renders a distinct dashed vertical line exactly at the day boundary, independent of the (disabled) grid.

**Phase 5c - Color scheme picker (Colors tab): SHIPPED, live-verified** (commit `19e4899`). New `"mt5-green"` `ChartTheme` in `canvas-colors.ts` - MT5's real "Green on Black" built-in scheme, verified against the user's own live Properties-dialog Colors-tab screenshot (Background Black, Foreground White, Grid/Bid-line LightSlateGray, Bar up/Line-chart/Volumes in the green family, Last-price-line's exact literal RGB(0,192,0)). Bear candle uses White rather than a separate outline color, reusing the "outline == fill" simplification the "at24" theme already had. Added to the same Chart Properties modal as a "Colors" select (`COLOR_SCHEMES` deliberately excludes `"at24"`, the platform's own generic theme, not an MT5 scheme). `colorScheme` is local `NativeChart` state defaulting to `"mt5"` - zero visual change until Properties is opened. Live-verified on production: switching to "Green on Black" turned bullish candles lime-green, bearish candles white, and the current-price marker green (confirmed via zoomed screenshot).

**Phase 5d - Save as Picture (PNG export): SHIPPED, live-verified** (commit `8b8ac5f`). New "Save Image" toolbar button calls `canvas.toDataURL("image/png")` on the SAME canvas the chart already renders into (captures the price panel, any active sub-panels, indicators, and drawn objects in one shot, since `renderChart` draws everything into that one canvas) and triggers a plain `<a download>` click - purely client-side, no server round-trip. Live-verified on production via direct `canvas.toDataURL()` execution: returned a genuine ~59KB base64 PNG at the canvas's real pixel dimensions, no tainted-canvas security error.

**Cross-session note:** a concurrent Claude session's commit (`981425d`, unrelated marketplace/licensing work) broke the shared Vercel build with a genuine TypeScript error, blocking deploys for ~40 minutes including two of this phase's own commits (5c, 5d) until that session pushed its own fix (`c812fec`). Production was never affected (Vercel kept serving the last successful deploy throughout); this phase's commits deployed automatically once the shared build was unblocked.

**Phase 5e - Object List panel (Ctrl+B): SHIPPED, live-verified** (commit `81ac4b8`). MT5's own Object List: an "Objects" dropdown in `DrawingToolbar.tsx` listing every drawn object, ordered oldest-first (`createdAt`), each row showing its real drawn color as a swatch plus a "#N ToolLabel" name (a horizontal line also shows its real anchor price inline, e.g. "@ 1.367519..."). Select and Delete are distinct per-row callbacks (select reuses the EXISTING `selectedDrawingObjectId` rendering path, zero renderer changes needed); deleting only clears the current selection when the deleted object WAS the selected one. Same dropdown dismissal pattern (own state/ref, Escape + outside-click) the Indicators/Templates dropdowns already use. Live-verified on production end-to-end: drew a trend line, confirmed it listed correctly with its orange color swatch, selected it (row highlighted), deleted it via the row's × (list and canvas both updated, count dropped to 0, empty state returned); separately drew a horizontal line and confirmed its row showed the real anchor price inline, matching the pink line drawn on the canvas at that exact price.

`scripts/validate-native-chart-phase5.ts` grew from 12 to 40 assertions across all five sub-slices (5a-5e), mirroring Phase 2's one-file-per-phase indicator test pattern.

**Phase 5 status: COMPLETE.** All five sub-slices (chart type, Properties panel, color schemes, Save as Picture, Object List) shipped and live-verified on production.

## D2.7.11 roadmap status: COMPLETE

Every phase (1, 1b, 2, 3, 4, 5a-5e) is shipped and live-verified on production. Post-completion, the user asked for two further items from their own numbered list, both now also shipped and live-verified: Bill Williams' tools (deferred from Phase 2) and Tiling durable persistence (promoted from Phase 3's session-scoped storage). The only remaining named candidate is the future paper-trading epic this chart is now positioned as the foundation for - explicitly gated on its own dedicated scoping/design conversation before any code, not something to start unprompted.

### Post-completion addition - Bill Williams' tools: SHIPPED, live-verified (2026-08-21)

The deferred Phase 2 item, taken up as roadmap item 1 after the user asked to proceed through their own numbered list. Alligator (Jaw/Teeth/Lips SMMA, MT5's real 13/8/5 periods with +8/+5/+3 bar future shifts), Awesome Oscillator (5/34-period median-price SMA difference, new sub-panel), and Fractals (5-bar structural extremes, rendered via the existing "dots" line style) - every default verified against metatrader5.com's own Bill Williams' Indicators help pages. Commit `575a199`, 19 new tests (`scripts/validate-native-chart-bill-williams.ts`), zero regressions across all 12 native-chart suites.

**A real, previously-undetected bug found and fixed during this live-verification** (commit `c479402`): Canvas 2D's `strokeStyle`/`fillStyle` silently reject CSS `var(--x)` custom-property syntax - assigning an unparseable color leaves the PREVIOUS strokeStyle/fillStyle unchanged, per spec. Every indicator whose `IndicatorLine.color` was a literal `"var(--x)"` string - EMA/SMA/RSI, Ichimoku's Kijun/Chikou, MACD's signal line, and now Alligator - had silently drawn in whatever color the last successful assignment happened to be, never its own real intended token color. This was NOT introduced by Bill Williams' tools - it dates back to Ichimoku (D2.7.3) - Alligator's three same-shade lines were just what finally surfaced it under pixel-level scrutiny. Fixed with `resolveIndicatorColor()` (`canvas-colors.ts`), applied at the actual draw boundary (`drawLine`/`drawDots` in `sub-panel-renderer.ts`) - the token-reference layer (`compute.ts`/`panel-registry.ts`) correctly keeps writing `"var(--gold)"` etc as the real semantic reference; only the thing handing a color to the canvas context needed to resolve it first.

**Live-verified via direct pixel-sampling on production** (not just a screenshot - screenshots had proven unreliable for judging subtle color differences earlier this session): enabled EMA-20 + Alligator + Awesome Oscillator together, scanned the live canvas's real pixel buffer via `getImageData`, and confirmed all three expected real colors are genuinely present - gold `#d4af37`/(212,175,55) 311px, steel `#94a3b8`/(148,163,184) 107px, gold-strong `#e7c86b`/(231,200,107) 130px. Before the fix, none of these appeared anywhere on the canvas. The Awesome Oscillator histogram's white/black bars are expected, pre-existing `"mt5"`-theme behavior (that theme's `colors.bullish`/`colors.bearish` are white/black, matching MACD's own histogram, which reuses the same two fields) - not a bug, not something this fix changed.

### Post-completion addition - Tiling durable persistence: SHIPPED, live-verified (2026-08-25)

Roadmap item 2 from the user's own numbered list: promoted the Phase 3 multi-symbol tiled layout (`provider`/`layout`/`panes`/`primaryPaneIndex`) from sessionStorage-only (tab-scoped, gone on tab close/new device) to a real durable per-user database table (`ChartWorkspaceLayout`), the exact Phase 1 -> 1b precedent already established for drawn objects. New `services/chart/chart-workspace-layout.service.ts` (validates every field via `chart-session-state.ts`'s own exported sanitizers - the same rules that guarded the old sessionStorage tier now guard this network boundary), `GET`/`PUT /api/private/chart-workspace-layout`, and `lib/chart-engine/chart-workspace-layout-store.ts` (client fetch wrapper, never throws). `chart-session-state.ts` is now a pure types+sanitizers module - its own sessionStorage I/O is genuinely retired, not left as dead code. Commit `e8d0a24`, 14 new tests (`scripts/validate-chart-workspace-layout-persistence.ts` - real Postgres for the service, fetch-mocked for the client store), plus 5 stale structural assertions across three other suites re-derived for the new architecture (never blindly deleted). Full regression sweep clean across all 13 native-chart suites.

**Live-verified end-to-end on production, via direct DOM inspection (not just the API layer):** set the layout to 2x2 with a distinct per-pane symbol via the real UI, confirmed the durable row via a direct `fetch('/api/private/chart-workspace-layout')` call from the page (`layout: 4`, the exact 4-pane array, `provider: "native"`), then did a genuinely fresh page load (new tab) and confirmed via DOM inspection that the "2×2" layout button, the "Native (Beta)" provider button, and all 4 panes' real symbols (`ETHUSD, EURUSD, ETHUSD, ETHUSD`) restored exactly matching the database row - not a screenshot-based check, an actual `aria-pressed`/`placeholder` DOM read. Also confirmed via a DOM-node-identity probe (a custom `data-probe` attribute surviving unchanged across several seconds) that `ChartPanel`/`ChartPane` were NOT remounting - ruling out a false read.

**Observation, not a bug:** the restore took noticeably longer to visually settle in this specific verification session than the underlying GET/PUT calls' own latency alone would suggest (each manual `fetch()` call independently resolved in 481ms-2.6s). This coincided with unusually heavy concurrent test traffic (several manual fetches, tab creations, and page reloads within a short window during this same verification pass) and broader Claude-in-Chrome tooling instability observed the same session (the screenshot tool errored with an unrelated `params.clip.scale` deserialization bug, and `computer`-tool clicks silently failed to register on several attempts, requiring a fallback to `element.click()` via `javascript_tool`). The database round-trip itself, and the component's restore logic, are both independently proven correct (14/14 tests, and eventually stable production DOM state); a single real user reloading the page once should see the same fast restore Templates/Drawings already established as acceptable UX for this exact async-restore pattern. Worth a quick recheck if a future session sees this same symptom in isolation (i.e. without the confounding heavy test traffic).

### Post-completion addition - Key Levels engine (Resistance/Support/Invalidation/Breakout/Pullback): SHIPPED, live-verified (2026-08-26)

Not one of the two roadmap items - a separate, adjacent gap the user asked to be investigated after "ab hame sab data mil raha hai" (now that MT5 is primary, more data might be available): the AI Intelligence panel's "Key Levels" section had shown "Not available" for every field since Sprint D2.2, per a deliberate Phase 7 product rule at the time ("no synthetic trade setups, no invented support/resistance"). A dedicated investigation (documented in the `project_ai_intelligence_data_gaps_investigation` memory) found 6 of 7 "Not available" panel gaps genuinely unfixable via MT5 - but Key Levels was the one exception: a real, standard technical-analysis calculation (recent range + Fibonacci retracement) that had simply never been built, not a data-provider limitation. Reversing the Phase 7 rule for this one field required its own explicit sign-off rather than proceeding under a general "do what's best" authorization already in force - the user gave that explicitly: **"Reverse karo aur build karo."**

`recentPriceRange()` (`lib/market-data/indicators.ts`) - a real 20-bar high/low, excluding the still-forming latest candle, matching `market-state.service.ts`'s own `BREAKOUT_LOOKBACK_BARS` exactly (same real "recent range" definition, never a second one). `keyPriceLevels()` - resistance/support are exactly that range's high/low; pullback is the standard 61.8% ("golden pocket") Fibonacci retracement between them, the same ratio `FIBONACCI_LEVELS` (`lib/chart-engine/drawing/types.ts`) already uses for the chart's own Fibonacci Retracement tool, deliberately redefined locally rather than imported, to keep `indicators.ts` (pure, server-safe) from depending on the client-side chart-drawing domain.

Wired into **both** intelligence pipelines with this exact same shared math, so the legacy `CopilotAnalysis` panel (Trading Copilot) and the `DecisionContext`/Workspace panel (the one in the user's screenshots) can never disagree for the same symbol - `intelligence-panel-projection.service.ts`'s own header comment documents a real prior bug where these two pipelines DID disagree, which made shared math a hard requirement here, not a nice-to-have. Invalidation/breakout are gated on a real EMA20/EMA50-derived directional bias - honestly `undefined` (rendered "Not available") without one, never guessed; with a bullish bias, invalidation = support and breakout = resistance, mirrored for bearish.

Files: `lib/market-data/indicators.ts`, `types/technical-context.ts`, `types/intelligence-panel.ts`, `services/ai/technical-context.service.ts`, `services/ai/intelligence-panel.service.ts`, `services/intelligence/chat/intelligence-panel-projection.service.ts`, `components/workspace/IntelligencePanel.tsx` (real values now render via `formatPrice`; caption rewritten to describe the real methodology). New `scripts/validate-key-levels-engine.ts` (13 assertions); rewrote the stale `keyLevels` test in `validate-intelligence-panel-projection.ts` (test 17 -> 17, 17a-17e) and a stale exact-import-string assertion in `validate-native-chart-production.ts` (test 13). `docs/market-data-layer.md`'s no-fabrication rules updated to describe the real, computed (not fabricated) methodology. Commit `874bf63`. Full regression: 24 suites, `tsc --noEmit`, `eslint .` all clean.

**Live-verified on production** (`www.algotraders24.ai/dashboard/workspace`, real `mt5` provider data): XAUUSD with a neutral bias showed Resistance 4,673.709 / Support 4,605.31 / Pullback 4,631.43842 (= `4673.709 - 0.618*(4673.709-4605.31)`, exact) with Invalidation/Breakout both honestly "Not available"; BTCUSD with a real bullish bias (EMA20 78157.88 > EMA50 77213.42) showed Resistance 79,236.86 / Support 75,850.03 / Pullback 77,143.79906 (exact same formula) with Invalidation = Support (75,850.03) and Breakout = Resistance (79,236.86) - confirming both the neutral-gating and bullish-bias branches live, against real data, not just the local test suite.

### Post-completion addition - SMC Liquidity Zones + momentum-divergence opposing evidence: SHIPPED, live-verified (2026-08-26)

User reviewed screenshots of three specific AI Intelligence/Research panel gaps after Key Levels shipped and asked to fix whatever was genuinely code-solvable rather than architecture-blocked: "OPPOSING: None available", "Liquidity zone data is not implemented", and the Prediction Window/Invalidated If box. Investigation found the third was already real, non-templated content (no fix needed); the first two split into one genuinely buildable-from-existing-data feature each - both shipped, commit `a1251f5`.

**Liquidity Zones (SMC Equal High/Low).** A real, price-action-derived proxy - deliberately NOT genuine order-book depth, which still doesn't exist anywhere in this platform's providers. Directly ported from the user's own tested `ea-research/G01_LiquiditySweep_MSS_FVG/Include/AT24_G01_Liquidity.mqh` (`G01_DetectEqualHigh`/`G01_DetectEqualLow`: scan swing-point pairs from most recent backwards, first pair within a tolerance wins, reported level is the pair's own real extreme) and its own tuned `InpEqualLevelATRTolerance` (0.10 x ATR14) - the user's own already-validated EA parameter, not a fresh guess. `liquidityZones()` (`lib/market-data/indicators.ts`) reuses the existing, tested `fractalsSeries()` (Bill Williams' real 5-bar structural-extreme definition) as its swing-point source. `types/intelligence-market-state.ts`'s `MarketStateStructure.liquidityZones` field - previously an always-`undefined` `unknown[]` placeholder explicitly declared "so a future sprint with a real data-backed proxy can populate it" - is now that real, typed field. Threaded through `market-state.service.ts` -> `DecisionCurrentState` -> `VerifiedAnswerResponse` -> `IntelligencePanelData` -> a new "Liquidity Zones (SMC)" section in `IntelligencePanel.tsx`, clearly captioned as price-action-derived, never order-book depth. `decision-context.service.ts`'s `buildMissingInformation()` changed the old unconditional "Liquidity zone data is not implemented" claim to a conditional one (present only when no real cluster is found), matching the existing `recentRange` pattern.

**Momentum-divergence opposing evidence.** The existing `opposingFromConflicts()` (`hypothesis.service.ts`) requires two evidence items of the *same* type from *different* sources that genuinely disagree - structurally near-unreachable with this platform's single-market-data-provider-per-analysis pipeline, which is why "OPPOSING: None available" was the near-permanent default. New `momentumDivergenceOpposingEvidence()` adds a second, independent real source that needs no second provider at all: RSI14 disagreeing with a directional hypothesis's own claim (overbought against a bullish continuation, oversold against a bearish one), using this codebase's own existing 70/30 RSI convention (already used by the native chart's RSI panel and `technical-context.service.ts`'s own observation text - not a fresh threshold). Wired into `trend-continuation-bullish/bearish` and `breakout-confirmation-bullish/bearish` only, where "divergence against the claim's own direction" is a coherent concept - `range-continuation` and `volatility-expansion/contraction` deliberately excluded.

New `scripts/validate-liquidity-zones-engine.ts` (10 assertions: pure engine, `MarketStateService` wiring, both panel pipelines' passthrough). Rewrote stale assertions across 7 other suites that encoded the old "`liquidityZones` always undefined" / "opposing evidence has exactly N items" assumptions (`validate-market-state`, `validate-hypothesis-engine`, `validate-dukascopy-capability-audit`, `validate-microstructure-poc`, `validate-market-data-integrity`, `validate-intelligence-data-sufficiency`, `validate-microstructure-context-integration`) - re-derived to match real, now-conditional behavior, never blindly deleted. Full regression: 30+ suites, `tsc --noEmit`, `eslint .` all clean.

**Live-verified on production** (`www.algotraders24.ai/dashboard/workspace`, real `mt5` data, via the real `/api/private/intelligence/research` endpoint queried directly for all 7 tracked instruments): Liquidity Zones showed real Equal High/Low values for 6 of 7 instruments (EURUSD 1.16758/-, GBPUSD 1.36408/1.36246, USDJPY -/158.926, XAGUSD -/67.544, BTCUSD 79236.86/78603.58, ETHUSD 2533.62/-), and XAUUSD honestly showed `{}` (no real cluster currently detected) - the old unconditional "not implemented" missing-info line is confirmed gone, replaced by the correct conditional one (absent for BTCUSD, which has a real cluster). Momentum-divergence: live RSI14 across all 7 instruments at verification time (30.47-66.91) never crossed the 70/30 thresholds against its own trend direction, so every instrument correctly returned `opposingEvidence: []` - the honesty-gating working as designed, not a bug; the positive case (a real overbought/oversold claim actually appearing) is proven by 3 dedicated tests in `validate-hypothesis-engine.ts` using deterministic RSI~87/oversold fixtures, since live market conditions didn't happen to produce an extreme RSI reading against trend on any tracked symbol at verification time.

## Working agreement for this roadmap

- Each phase ships as its own tested, reviewed unit - never several phases half-done at once.
- Every new drawing object/indicator reuses the EXISTING coordinate-system/viewport/panel-layout primitives (`priceToY`/`timeToX`/`Viewport`/`PanelRow`) - never a second geometry system.
- No phase touches order execution, ever.
- Research claims (MT5's real default behavior for a given tool) are grounded against mql5.com/metatrader5.com before being implemented as fact, matching this session's own research-first discipline - a cosmetic choice (e.g. a drawing tool's default color) is never presented as "this is MT5's real default," only a genuine platform behavior claim is.
