# Q1.7 Part 1 — Release Baseline

Captured at the start of this sprint, before any Q1.7 change.

## Git state

- **HEAD commit**: `b22af4b51ad6e32c8e771d7a63eba211f67c7453` — "Merge pull request: Q0-Q1.3 AT24 Quant Engine (feat/q13-pending-order-management)", 2026-08-31 15:56:51 +0530, branch `main`.
- **Working tree**: 160 changed/untracked paths at sprint start — this whole program (Q1.4 through Q1.6, plus the reports/scripts from Q0.6-Q1.3 not yet committed) remains uncommitted, per the standing "no auto-commit" rule. Nothing in this count is new to Q1.7.

## Test count

**375/375** pre-existing automated assertions, all passing at sprint start:
- Live-API suites (require the dev server): Q0.9 `q09_backend_tests.ts` 28, Q1.1 `q11_gap_policy_tests.ts` 25, Q1.2 `q12_tests.ts` 25, Q1.3 `q13_tests.ts` 65, Q1.4 `q14_codegen_tests.ts` 28, Q1.5 `q15_security_tests.ts` 67 = **238**.
- Python-side suites: Q0.9 `q09_regression_test.py` 22, Q1.4 `q14_golden_strategies.py` 42, Q1.4 `q14_indicator_matrix_test.py` 73 = **137**.
- 238 + 137 = **375**, matching the sprint brief's expected baseline exactly.

## Build status

`npm run build` — exit 0, clean, no errors.

## Typecheck status

`npx tsc --noEmit` — **11 pre-existing errors**, all confined to `frontend/scripts/q14_codegen_tests.ts` (TS global-script-scope collisions with sibling test scripts `q09`/`q11`/`q12`/`q13` that lack `export {}`, plus 5 unrelated `provenance`-property errors in that same file) — documented in `Q1.5_BETA_READINESS.md` and reconfirmed unchanged in `Q1.6_COMPLETE.md`. Not touched this sprint's baseline capture.

## Quant Lite routes

| Route | Purpose |
|---|---|
| `/quant-lite` | Public landing page |
| `/quant-lite/builder` | Strategy Builder |
| `/quant-lite/backtest` | Backtest Setup |
| `/quant-lite/results/[id]` | Running/Results |
| `/quant-lite/library` | Strategy Library (legacy sample) |
| `/quant-lite/library/[id]` | Strategy Library detail |
| `/quant-lite/upgrade` | Lite-vs-Pro comparison |

Reachable from the public site via the Navbar's "Quant Lite" item and the Footer's "Quant Lite (Free)" item (added Q1.6).

## API routes

| Route | Method | Purpose |
|---|---|---|
| `/api/quant-lite/backtest` | POST | Submit a backtest job |
| `/api/quant-lite/backtest/[jobId]` | GET | Poll/fetch a job's status and result |
| `/api/quant-lite/coverage` | GET | Server-authoritative data-coverage assessment |
| `/api/quant-lite/codegen` | POST | Generate MT4/MT5/Pine code from a strategy spec |

## Codegen state

`quant-engine/spec_engine/codegen_mql4.py`, `codegen_mql5.py`, `codegen_pine.py` — all three verified in Q1.4 (parity matrix, golden strategies, 10-indicator matrix) and re-verified in Q1.5 (67 security/boundary assertions, one real defect found and fixed: undeclared-ref validation gap in `schema.py`'s `_validate_ref()`). `GENERATOR_VERSION = "q1.4-codegen-v1"`. No compiler available in this environment — every artifact is `STATIC-VALIDATION-ONLY`, stated explicitly, never claimed otherwise.

## execution_mtf.py state

Untouched by any sprint since Q0.6 (the canonical look-ahead fix). Its current uncommitted diff against the last commit (`+20/-8` lines) is that same Q0.6 fix, carried uncommitted through every subsequent sprint (confirmed identical in Q1.4/Q1.5/Q1.6's own baseline checks) — not a Q1.7 change, not modified this sprint.

## market.db baseline (pre-sprint)

SHA-256 `609cdc21f88629569bbd947b2f7fd7ed8cf5e9799800957c15a2435b35df4aae`, size `2,053,308,416` bytes, mtime `1787561280.3769186` — identical to every prior sprint's recorded baseline. Will be re-verified byte-identical at sprint end (Part 14).

## strategy_library.db baseline (pre-sprint)

Untouched, confirmed via `git status --porcelain` (no diff for this path).
