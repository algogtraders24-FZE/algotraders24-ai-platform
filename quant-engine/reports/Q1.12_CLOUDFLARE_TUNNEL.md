# Q1.12 Phase 4 — Cloudflare Tunnel Audit & Route Addition

## Status: PASS — real config read, real route added, real public hostname verified live

## Existing tunnel (read-only inspection, before any change)

```yaml
tunnel: 18d9ea00-4478-411e-996a-d415a19e9e5b
credentials-file: C:\Users\ALGVPS121\.cloudflared\18d9ea00-4478-411e-996a-d415a19e9e5b.json

ingress:
  - hostname: mt5.algotraders24.ai
    service: http://localhost:8787
  - service: http_status:404
```

`cloudflared.exe` at `C:\cloudflared\cloudflared.exe` (2 processes — a supervisor/worker pair, same pattern as the service itself). Managed via 3 existing scheduled tasks (`AT24-MT5-Bridge`, `AT24-MT5-Tunnel`, `AT24-MT5-Watchdog`) plus one legacy-named `AT24 MT5 Bridge` entry (not used this sprint).

## Route added — one new ingress rule, existing `mt5.algotraders24.ai` entry untouched

```yaml
tunnel: 18d9ea00-4478-411e-996a-d415a19e9e5b
credentials-file: C:\Users\ALGVPS121\.cloudflared\18d9ea00-4478-411e-996a-d415a19e9e5b.json

ingress:
  - hostname: mt5.algotraders24.ai
    service: http://localhost:8787
  - hostname: quant-lite-exec.algotraders24.ai
    service: http://localhost:8788
  - service: http_status:404
```

Backup of the original config saved (`config.yml.backup-q112`) before editing. **No second tunnel created** — this new hostname rides the exact same existing named tunnel, per this sprint's own explicit preference.

## DNS

`cloudflared.exe tunnel route dns 18d9ea00-4478-411e-996a-d415a19e9e5b quant-lite-exec.algotraders24.ai` — real output: `"Added CNAME quant-lite-exec.algotraders24.ai which will route to this tunnel"`.

## Tunnel restart — and confirmation MT5 was not disrupted

`Stop-ScheduledTask`/`Start-ScheduledTask` on `AT24-MT5-Tunnel` (the tunnel process itself, not `AT24-MT5-Bridge` — the bridge's own local service was never touched). Immediately after: `https://mt5.algotraders24.ai/health` re-checked and confirmed unchanged — `{"status":"ok","mt5_connected":true,"account":127726651,"server":"Exness-MT5Real7"}`. The brief reconnect window (a few seconds) caused no observed disruption.

## New public hostname — verified live, twice, independently

1. From the VPS itself: `Invoke-RestMethod -Uri "https://quant-lite-exec.algotraders24.ai/health"` → `{"status":"ok","secretConfigured":true,"enginePresent":true,"marketDbPresent":true}`.
2. From this agent's own environment (genuinely external — a `curl` call made from outside the VPS entirely, not a proxy or simulation): the exact same response, independently confirmed.

## No new public VPS port

The execution service still binds to `127.0.0.1:8788` only — reachability to `quant-lite-exec.algotraders24.ai` is entirely through the tunnel, exactly the same pattern the MT5 bridge's own `mt5.algotraders24.ai` already uses. No inbound firewall rule was added or needed.
