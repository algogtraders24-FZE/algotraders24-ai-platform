# Q1.12 Phase 6 — Real Tunnel Test (the acceptance test)

## Status: PASS — the actual, complete Vercel-independent path proven, live, through the real public internet

**Explicitly not a localhost test.** Every request in this report went to `https://quant-lite-exec.algotraders24.ai`, resolved via real public DNS, routed through Cloudflare's real edge, tunneled to the real VPS.

## MACD — through the real tunnel

```
Trades: 208, Win rate: 43.75%, Profit factor: 1.15, Return: +19.61%, Final balance: $11,961.23
```

**Exact match** to every prior sprint's baseline (Q0.9 through Q1.11, and now the VPS's own local-script runs in Q1.10). Round-trip time (submit to completed, including two real internet hops each way): ~45s total across submit + poll (the engine's own computation time dominates this, not network overhead — consistent with Q1.10's own local timing for this exact spec).

## RSI — through the real tunnel (also used for the auth/failure tests)

```
Trades: 227, Win rate: 35.68%, Profit factor: 1.1, Return: +14.55%, Final balance: $11,455.00
```

**Exact match.**

## What this proves, concretely

```
[Real request, this agent's own environment / VPS PowerShell]
  ↓ HTTPS, real DNS resolution
Cloudflare edge (real, not simulated)
  ↓ real tunnel connection
VPS quant_lite_execution_service.py (real, persistent, SYSTEM-account process)
  ↓ spawns the unmodified run_backtest_job.py
execution_mtf.py (unmodified canonical engine)
  ↓ reads
market.db (real, checksum-verified, read-only)
  ↓
Real result, exact parity to every prior sprint
```

Every arrow above is now real — this is the strongest evidence this entire multi-sprint track has produced. `/health` was independently confirmed reachable from this agent's own environment (a genuinely separate, external machine, not the VPS and not a proxy).

## EMA (losing strategy) — through the real tunnel

```
Trades: 121, Win rate: 31.4%, Profit factor: 0.91, Return: -7.5%, Final balance: $9,250.00
```

**Exact match.** A negative-result strategy, reproduced correctly through the full real path.

## 2 concurrent jobs — through the real tunnel, real MT5 running simultaneously (Phase 10)

EMA (above) and a second, distinctly-configured RSI job submitted together, real tunnel, both processed concurrently. **Zero cross-contamination**: the EMA job's result is exactly the EMA baseline; the RSI job (a deliberately different threshold, not one of the fixed golden specs) produced its own distinct, internally-consistent result (232 trades, 35.78%, PF 1.1) — no mixing between the two. `https://mt5.algotraders24.ai/health` checked immediately after both completed: **`mt5_connected: true`**, completely unaffected by the concurrent Quant Lite load running through the same VPS and the same shared tunnel infrastructure.

## What is NOT yet proven (still requires Phase 7, out of this agent's own reach)

The **Vercel** leg specifically. `algotraders24.ai/quant-lite` still 404s (the frontend branch has never been merged), and even once it is, Vercel's own production environment doesn't yet know `quant-lite-exec.algotraders24.ai` exists — `QUANT_LITE_EXEC_SERVICE_URL`/`QUANT_LITE_EXEC_SECRET` have not been set in Vercel's dashboard (this agent has no access to do so). The service side of the architecture is now fully real and proven; the Vercel side remains exactly where Q1.11 left it — built, tested locally, never pointed at a real deployed target.
