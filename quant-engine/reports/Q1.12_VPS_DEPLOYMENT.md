# Q1.12 Phase 2 — VPS Service Deployment

## Status: PASS — real, deployed, tested live this sprint

Service files (`quant_lite_execution_service.py`, `requirements.txt`) transferred to `C:\AT24\quant-lite\quant-engine\service\` (RDP drive redirection, same method as Q1.10) — copy verified byte-identical (18,374 bytes both sides).

## Environment verified on the real VPS

```
Python 3.11.0
fastapi 0.141.1 | uvicorn 0.52.4 | numpy 2.3.5 | pandas 2.3.3
```

numpy/pandas exactly match Q1.10's pinned versions (no drift). fastapi/uvicorn resolved newer than this file's own floor (`>=0.110.0`/`>=0.29.0`) — within the declared semver range, not a mismatch.

## market.db re-checksummed immediately before starting the service

`609cdc21f88629569bbd947b2f7fd7ed8cf5e9799800957c15a2435b35df4aae` — matches every prior sprint's record.

## Service started, real, on the actual VPS

`uvicorn quant_lite_execution_service:app --host 127.0.0.1 --port 8788` — started cleanly, no errors. `/health` (unauthenticated, matching `mt5-bridge`'s own design): `{"status":"ok","secretConfigured":true,"enginePresent":true,"marketDbPresent":true}`.

## Real backtests through the real VPS service — 3 for 3, exact parity

| Strategy | Trades | Win Rate | PF | Return | Final Balance | Match |
|---|---|---|---|---|---|---|
| MACD | 208 | 43.75% | 1.15 | +19.61% | $11,961.23 | ✅ exact |
| RSI | 227 | 35.68% | 1.1 | +14.55% | $11,455.00 | ✅ exact |
| EMA Crossover | 121 | 31.4% | 0.91 | -7.5% | $9,250.00 | ✅ exact |

**This is the first time this whole track's HTTP service has been proven against the real VPS** rather than a localhost proxy — Q1.11's service code, unmodified, deployed and run for real.

## No duplicate engine implementation

The service imports and spawns `run_backtest_job.py`/`execution_mtf.py` directly — confirmed by the deployment script's own read-only grep before starting anything, and by the fact that the results above are byte-for-byte identical to every prior sprint's own direct-script runs (a reimplementation would have no reason to reproduce these exact numbers).

## What remains for Phase 2/3

The service is currently running in a foreground PowerShell window (started this sprint, for this test) — not yet a persistent, restart-surviving process. `Q1.12_COMPLETE.md`'s Phase 3 section covers the Task Scheduler conversion, next.
