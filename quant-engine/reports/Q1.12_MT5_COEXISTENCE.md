# Q1.12 Phase 10 — Concurrency + MT5 Coexistence (through the real tunnel)

## Status: PASS — real, live, through the actual public path this time (not the localhost proxy Q1.10 used)

## 1 remote backtest — Cloudflare Tunnel active, MT5/bridge running normally

MACD and RSI both run individually through `https://quant-lite-exec.algotraders24.ai`, both exact matches to established baselines (`Q1.12_PRODUCTION_E2E.md`). No coexistence issue observed at any point.

## 2 concurrent remote backtests — the harder case

EMA + a distinctly-configured RSI job submitted together through the real tunnel. Both completed correctly:

| Job | Trades | Return | Contamination? |
|---|---|---|---|
| EMA | 121 | -7.5% | None — matches its own established baseline exactly |
| RSI (concurrent) | 232 | +15.72% | None — internally consistent with its own distinct threshold, not mixed with EMA's numbers |

## MT5 / bridge impact

`https://mt5.algotraders24.ai/health` re-checked immediately after the 2-job concurrent run: `{"status":"ok","mt5_connected":true,"account":127726651,"server":"Exness-MT5Real7"}` — **identical to every prior check this sprint**, no disconnect, no degradation observed.

## Additionally, earlier this sprint (Phase 4): a tunnel restart, and its own MT5 impact

Adding the new ingress route required restarting the shared `AT24-MT5-Tunnel` scheduled task. `mt5.algotraders24.ai/health` was checked immediately before and after — no change in reported status. The brief reconnect window this causes is a one-time cost of adding the route, not a recurring characteristic of running Quant Lite traffic.

## No orphan processes

Confirmed earlier this sprint during the Task Scheduler debugging (`Q1.12_VPS_DEPLOYMENT_PHASE3.md`): once the stale original process was properly terminated, exactly one execution-service process remains bound to port 8788 at any time — re-confirmed via `Get-NetTCPConnection` before this sprint's final tests.

## What was not separately re-measured this sprint

Peak RAM/CPU during the concurrent run specifically (Q1.10's own coexistence test already measured this in detail for a single heavy job; this sprint's own priority was proving the *network path* carries concurrent load correctly, which it does — a fresh RAM/CPU capture during concurrency is folded into `Q1.12_COMPLETE.md`'s own list of what remains for full "VERIFIED" status, not silently treated as already covered).
