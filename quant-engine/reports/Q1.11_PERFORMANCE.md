# Q1.11 Phase 9 — Formal Performance Benchmark

## Status: not run on the real VPS this sprint — the one required item this sprint did not complete

This sprint's own local testing (`Q1.11_EXECUTION_SERVICE.md`, `Q1.11_PARITY.md`) proves the service is **correct**, but correctness and performance are different questions, and this sprint's own Phase 9 explicitly wants 1h/5m/1m × 365-day timing measured **on the actual execution host**, each run at least twice, with peak RAM/CPU/disk-I/O/concurrent-process-count recorded there — not on this local development machine, which has different hardware than the VPS Q1.9 profiled (8GB RAM, 4-core Broadwell).

Q1.10 already has real 1m/365-day timing **from the VPS itself** (the coexistence-test job), but that was one run, for one purpose (measuring MT5 impact, not formally benchmarking the engine's own execution time), not the structured "run twice, record wall-clock/RAM/CPU/disk-I/O/concurrency" table this phase specifically wants.

## What exists from prior sprints (restated, not re-measured)

| Timeframe | Range | Source | Elapsed time |
|---|---|---|---|
| 1h | 6 months | Local dev machine (Q1.7) | ~2.8s |
| 5m | 90 days | Local dev machine (Q1.7) | ~41.1s |
| 1m | 30 days | Local dev machine (Q1.7) | ~21.1s |
| 1m | 365 days | **VPS itself** (Q1.10, single coexistence-test run) | Completed successfully, exact duration not isolated as its own metric in that test's own logging |
| 1m/5m | ~950 days (full history) | Local dev machine (Q1.1/Q1.3) | ~57-90s (real run-to-run variance already observed) |

## Q1.3's established threshold — unchanged, not touched this sprint

`LARGE_RUN_THRESHOLD_DAYS = 365` in `performanceClassification.ts` remains exactly as established — this sprint invented no new threshold and has no evidence that would justify changing it either way.

## What this sprint's formal benchmark still requires

Run, on the actual VPS, through the actual HTTP service (not a direct script call, so the network/service overhead is included in the measurement):

```powershell
# Once the service is deployed on the VPS (per Q1.11_EXECUTION_SERVICE.md's deployment section):
Invoke-RestMethod -Method POST -Uri "http://127.0.0.1:8788/backtest" -Headers @{Authorization="Bearer $env:QUANT_LITE_EXEC_SECRET"} -ContentType "application/json" -Body (<1h/365d config> | ConvertTo-Json -Depth 10)
# poll GET /backtest/{jobId}, record wall-clock from POST to COMPLETED
# repeat for 5m/365d, 1m/365d, each twice
# capture Get-Process's own CPU/RAM for the python child process during each run, same technique Q1.10 already used successfully
```

Not run this sprint — flagged as the one genuinely outstanding item from this sprint's own acceptance criteria, not silently marked complete.
