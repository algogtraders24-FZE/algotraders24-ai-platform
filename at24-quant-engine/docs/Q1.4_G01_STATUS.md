# Q1.4 — G01 Status

## Re-verified, unchanged: `BLOCKED`

Re-ran `npx tsx --test test/isolation.test.ts test/mql-g01-import.test.ts` fresh against the current `main` (`b22af4b`) as part of this sprint's own audit: **17/17 pass, unchanged**. G01's real source file (`ea-research/G01_LiquiditySweep_MSS_FVG/AT24_GOLD_G01_LiquiditySweep_MSS_FVG.mq5`) was never opened for writing by Q1.4 — `git status --porcelain -- at24-quant-engine/src` (and `ea-research/`, per rule 7) confirm zero touches anywhere near it.

## Why it is blocked

G01's real entry/exit logic is a multi-bar, `#include`-spanning STATE MACHINE (Liquidity Sweep → Displacement → MSS → FVG → Retest) — a shape no version of this importer's `simpleEntryConditions` reconstruction (`if(indicatorA <op> indicatorB)`) has ever been able to represent. The honest, unchanged outcome: `entries[].condition` is the `UNREPRESENTABLE_CONDITION` placeholder (always evaluates false), paired with a `BLOCKING`-severity `UnsupportedSemantic`, which `checkReductionEligibility` — genuinely unmodified this sprint — correctly refuses.

## Exact blocking semantics

- `entry.direction === "FLAT"` (no resolvable direction) → `checkReductionEligibility` rejects.
- `provenance.unsupportedSemantics` contains a `BLOCKING`-severity "entry/exit signal logic" item, per `ir-generator.ts`'s own honest-placeholder rule (unchanged).

## Which parts are understood

- All 27 real input parameters extracted.
- All 4 real MQL5 event handlers recognized.
- Both `CTrade.Buy`/`CTrade.Sell` market entries recognized with SL/TP/volume expressions extracted (though the SL/TP FORMULA itself remains cross-file-unresolved).
- Genuine M5-execution + M15-higher-timeframe MTF usage recognized.
- The tick-driven, unconditional live-bid/ask entry trigger is correctly classified `REALTIME_DEPENDENT`, never falsely `NON_REPAINTING`.
- Real account/broker dependencies (`AccountInfoDouble`, `SYMBOL_TRADE_STOPS_LEVEL`/`FREEZE_LEVEL`) recognized and separated from strategy semantics.
- Zero `iCustom`/DLL/`WebRequest` usage — an honest negative, not an assumed one.

## Which parts are executable

None — the whole strategy remains `BLOCKED`, correctly, since its core entry/exit logic (the part that actually decides WHEN to trade) is the unresolved piece.

## Which parts remain unresolved

The cross-file, multi-bar state-machine entry/exit sequence itself — requires either (a) a full cross-file `#include` resolver plus a genuine multi-bar state-machine IR representation (explicitly deferred, rule 12/15), or (b) hand-authoring G01's logic directly as a `StrategySpec` (bypassing MQL import entirely, a legitimate but separate path — see `Q1.4_ELIGIBILITY_GATE_AUDIT.md`'s "alternate path" nuance).

## Did Q1.4's own changes affect G01 in any way?

No. Q1.4 adds zero new detection logic and modifies zero `src/` files. G01's 17/17 test outcomes are byte-for-byte identical to the pre-Q1.4 baseline. Per rule 36, G01 remains honestly `BLOCKED` — this sprint made no attempt, and found no accidental side-effect, that would change that.
