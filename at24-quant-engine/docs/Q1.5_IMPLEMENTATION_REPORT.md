# Q1.5 — Implementation Report (Phases 2–5)

Universal Semantic Gap Closure & Position-Policy Enforcement. Branch `feat/q15-semantic-gap-closure`, base `origin/main` @ `864ff58` (Q1.4 merged). Scope: `at24-quant-engine/**` only.

## Status

| Phase | Item | Status |
|---|---|---|
| 2 | MQL5 `OrderGetInteger(ORDER_TYPE)` | **DONE** — see `docs/Q1.5_ORDER_TYPE_SEMANTICS.md` |
| 5 | MQL4 `MarketInfo()` classification | **DONE** — see `docs/Q1.5_MARKETINFO_CLASSIFICATION.md` |
| 4 | Pyramiding / `maxEntries` / `maxPositions` | **DONE** — see `docs/Q1.5_PYRAMIDING_POLICY.md` |
| 3 | Generic `SIGNAL_EXIT` | **DONE** — see `docs/Q1.5_EXIT_CONTRACT.md` |
| — | D1/D2/D3 parity | **DONE** — fallback-path AND genuine non-empty D2/D3 intrabar coverage (verification closure, see `docs/Q1.5_FIDELITY_PARITY.md`) |
| — | No-look-ahead | **DONE** — original 2 dedicated tests + 2 more from the genuine intrabar fixture |
| — | Determinism/immutability | **DONE** — 3-run hash equality, JSON round-trip, no-mutation proof |
| — | Pending-order + pyramiding interaction | **DONE** (verification closure) — 6 dedicated cases, `docs/Q1.5_PYRAMIDING_POLICY.md` |
| — | Dedicated adversarial corpus | **DONE** (verification closure) — 15 named cases, `test/q15-adversarial-corpus.test.ts` |
| — | Cross-platform contract (Pine/NinjaScript/cBot equivalence) | **NOT BUILT** — explicitly out of scope for both the implementation sprint and this verification closure |
| — | Performance benchmarking | **NOT BUILT** — explicitly out of scope |

## Verification Closure (second pass)

A follow-up sprint closed the remaining verification gaps flagged above (no engine changes — verification/testing only):
- `test/fixtures/q15-pyramiding-exit-fixtures.ts` extended with a genuine, non-empty `BarDetailProvider` fixture (H1 parent / M15 child, mirroring `fidelity-fixtures.ts`'s Fixture-A pattern).
- `test/q15-intrabar-fidelity.test.ts` (7 tests) — real D2/D3 intrabar fills, `maxEntries` under real intrabar fills, `SIGNAL_EXIT` under the real D2/D3 path, D1-vs-D2 legitimate-difference proof, 2 look-ahead tests.
- `test/q15-pending-pyramiding-interaction.test.ts` (6 tests) — the 6 required pending+pyramiding cases.
- `test/q15-adversarial-corpus.test.ts` (15 tests) — the 15 named adversarial cases.
- Docs updated: `Q1.5_FIDELITY_PARITY.md`, `Q1.5_EXIT_CONTRACT.md`, `Q1.5_PYRAMIDING_POLICY.md` — each now distinguishes already-proven / newly-proven-this-sprint / still-not-proven.

## A genuine architecture contradiction found and corrected mid-sprint

Before implementing `maxEntries`, runtime tracing of `decision-builder.ts` proved the audit-stage assumption wrong: `docs/Q1.4_FUTURE_PLATFORM_CONTRACT_CHECK.md`'s claim that "unlimited accumulation actually happens" was inaccurate — pyramiding was **completely unreachable** through the normal decision pipeline pre-Q1.5, not merely uncapped. This was reported, the user explicitly authorized proceeding with the corrected understanding, and Q1.5.4 made pyramiding genuinely reachable for the first time (gated behind `allowPyramiding: true`) rather than merely adding a cap to something that already worked. Full detail and the corrected historical statement: `docs/Q1.5_PYRAMIDING_POLICY.md`.

## Files changed

**Source (`src/`)**:
- `runtime/mql-importer/semantic-analyzer.ts` — `OrderGetInteger(ORDER_TYPE)` recognition (exact-match); `MarketInfo()` broker-constraint classification.
- `domain/pending-order-management-policy.ts` — MQL5 order-type constants added to `MQL_ORDER_TYPE_CONSTANT_MAP`.
- `domain/strategy-ir/strategy-ir.ts` — `maxPositions`/`maxEntries` validation (`>= 1`).
- `domain/strategy-spec.ts` — new optional `pyramiding?: PyramidingPolicy` field (additive, Q0.13-precedented pattern).
- `domain/simulation/simulation-state.ts` — new `entryCountByPosition: ReadonlyMap<string, number>` field (additive).
- `runtime/reduction/ir-to-spec-reducer.ts` — `exitRules` now populated from `SIGNAL_EXIT`; `pyramiding` passthrough.
- `runtime/reduction/eligibility-gate.ts` — `SIGNAL_EXIT` eligible with a real condition; comment corrections.
- `runtime/strategy-ir/execution-compatibility-engine.ts` — `SIGNAL_EXIT` `SUPPORTED` with a real condition.
- `runtime/signal-generator.ts` — new `firstMatchingExitRule()`.
- `runtime/simulation/decision-builder.ts` — new `PyramidingAdmission` param on `buildDecision`.
- `runtime/simulation/simulation-engine.ts` (D1) — entry-count tracking, Step 1c (`SIGNAL_EXIT`), pyramid admission wiring, separate entry-risk evaluation.
- `runtime/fidelity/multi-fidelity-engine.ts` (D2/D3) — identical mirror of the above.

**Tests (`test/`)**: `q15-order-type-semantics.test.ts` (43), `q15-marketinfo-classification.test.ts` (11), `q15-pyramiding.test.ts` (18), `q15-signal-exit.test.ts` (10), `q15-fidelity-parity.test.ts` (4), `q15-determinism.test.ts` (3), `q15-intrabar-fidelity.test.ts` (7, verification closure), `q15-pending-pyramiding-interaction.test.ts` (6, verification closure), `q15-adversarial-corpus.test.ts` (15, verification closure), `fixtures/q15-pyramiding-exit-fixtures.ts` (shared fixture builder, extended with a genuine intrabar fixture in verification closure). Updated: `q14-mql-corpus.ts` (1 fixture description), `q14-end-to-end-pipeline.test.ts` (1 test flipped to assert the closed gap), `q09-eligibility-gate.test.ts` (1 flipped + 1 new), `strategy-ir-execution-compatibility.test.ts` (1 flipped + 1 new).

**Docs**: this file, `Q1.5_ORDER_TYPE_SEMANTICS.md`, `Q1.5_MARKETINFO_CLASSIFICATION.md`, `Q1.5_PYRAMIDING_POLICY.md`, `Q1.5_EXIT_CONTRACT.md`, `Q1.5_FIDELITY_PARITY.md`, `AT24_QUANT_ENGINE_MASTER_BLUEPRINT.md` (updated).

## Constraints honored

No HEDGING implementation. No IR/`ExitIR` redesign (both reused as-is, per the audit's own conclusion that they already supported the representation — only execution was missing). No second expression evaluator (`evaluateExpression` reused unchanged). No MQL-specific exit mechanism (`StrategySpec.exitRules` is platform-neutral by construction). No direct position mutation from `decision-builder.ts` (it only returns a `Decision`; all state changes happen through `position-engine.ts`'s existing primitives). `evaluateRisk()`/`RiskEvaluationInput`'s single-purpose-per-call contract preserved literally (two separate calls on a pyramid-entry bar, never merged). Zero Quant Lite / M-Series / G01 / DB / frontend / Paper Trading files touched.

## Test count

Baseline (Q1.4, post-merge): 1095/1095. After Phase 2+5: 1119/1119. After Phase 3+4 + eligibility/compatibility test corrections: 1121/1121. After the initial Q1.5 implementation sprint (including determinism/immutability): 1156/1156. After verification closure (+7 intrabar-fidelity, +6 pending-pyramiding-interaction, +15 adversarial-corpus = +28 net new, zero existing tests modified/weakened/removed): **1184/1184 passing**. Typecheck: PASS. Production build: PASS.
