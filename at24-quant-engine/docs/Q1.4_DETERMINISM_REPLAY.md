# Q1.4 — Deterministic Replay

`test/q14-provenance-identity-determinism.test.ts`'s REPLAY tests + `test/q14-fidelity-parity.test.ts`/`test/q14-negative-corpus.test.ts`'s determinism-adjacent proofs.

## 3 identical runs of a real, compiled, MQL-imported strategy

Compiled `mql4-08-orderselect-ordermodify` (a real fixture with an entry, SL/TP absence, and an executable pending-order-management rule), ran it 3 times against 150 synthetic FX bars through `compileToSimulation`:

- `simulationResultHash` — identical across all 3 runs
- `provenance.strategyHash` — identical across all 3 runs
- (implied by resultHash identity) execution decisions, order lifecycle, trades, and metrics are identical byte-for-byte, since `resultHash` is computed over the full result object

## Replay from the same canonical input

A JSON round-trip of the source text (`JSON.parse(JSON.stringify(sourceText))`) re-imported produces the identical `computeCanonicalIRHash()` — proving the pipeline's determinism survives a realistic serialization boundary (e.g. a strategy source stored and reloaded from a database or job queue), not merely two back-to-back in-process calls.

## Cross-file replay evidence (reused, not duplicated)

- `test/q13-determinism-provenance.test.ts` — 3-run IR translationHash identity, unmodified, re-verified passing.
- `test/q12-determinism-provenance.test.ts` — 3-run order-modification result identity, unmodified, re-verified passing.
- `test/q11-determinism-provenance.test.ts` — 3-run order-type result identity, unmodified, re-verified passing.
- `test/simulation-replay.test.ts` — Q0.5's own original replay proof, unmodified, re-verified passing.

## Verdict

**Determinism holds across the entire audited pipeline, including the newly-emphasized "replay from serialized input" case Q1.4 specifically asked for.** No nondeterministic source (`Date.now()`, `Math.random()`, unordered iteration affecting output) was found anywhere in the touched code — see `docs/Q1.4_DETERMINISM_AUDIT.md` for the static scan.
