# Q1.4 — Static Nondeterminism Audit

## Scope of this sprint's own touched code

Q1.4 modifies **zero** files under `src/` — confirmed via `git status --porcelain -- at24-quant-engine/src` (empty). Every file this sprint touches is new: test files, fixture files, and one benchmark script. The pre-existing static scan (`test/simulation-determinism.test.ts`'s "Q0.5.36" test, scoped to `src/runtime/simulation/*.ts`) is therefore unaffected and was re-run unmodified as part of the full 1079-test regression pass — still passing.

## Manual scan of every new Q1.4 file

```
grep -rEn "Date\.now\(\)|Math\.random\(\)|process\.env|new Date\(\)|crypto\.randomUUID" \
  test/fixtures/q14-mql-corpus.ts test/q14-*.test.ts scripts/benchmark-q14-full-pipeline.ts
```

**Zero matches.** No `Date.now()`, no `Math.random()`, no `process.env`, no `new Date()`, no `crypto.randomUUID` anywhere in this sprint's new code.

## Other nondeterminism categories checked

- **Hidden global state**: `test/q14-mql-corpus.ts` exports only pure functions (`mql4NoTrailer`/`mql5NoTrailer`) and readonly data (`Q14_CORPUS`) — no module-level mutable state.
- **Unordered iteration affecting output**: every corpus/benchmark test iterates `Q14_CORPUS` (a plain array, insertion-ordered) or fixed-length local arrays — no `Set`/`Map` iteration order dependency introduced. (Pre-existing `Map`-based state, e.g. `state.pendingOrders`, is Q0.5's own frozen, already-audited code — unmodified.)
- **External network/filesystem dependency**: none — every fixture is an in-memory string; `buildSyntheticFxBars` (reused, unmodified) is a pure deterministic generator.
- **Process-dependent behavior**: the new benchmark script uses `process.hrtime.bigint()` for TIMING MEASUREMENT ONLY (never fed into a simulation decision or hash) — the identical, already-accepted pattern every prior benchmark script (`benchmark-reduction.ts`, `benchmark-order-types.ts`, etc.) already uses.

## Verdict

**No nondeterminism risk introduced by Q1.4.** The pre-existing simulation-runtime static scan remains the authoritative, unmodified gate; this sprint's own new code independently passes the identical check.
