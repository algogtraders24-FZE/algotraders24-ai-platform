# Q1.4 — End-to-End Import Matrix

Every fixture below is defined in `test/fixtures/q14-mql-corpus.ts` and traced through the FULL canonical pipeline (SOURCE → PARSED → SEMANTIC RESULT → IR → COMPILED SPEC → ELIGIBILITY) by `test/q14-end-to-end-pipeline.test.ts` — one real, passing assertion per row, never hand-typed without a test behind it.

## MQL4 (1–10)

| # | Fixture | Parsed | Semantic result | IR | Eligibility |
|---|---|---|---|---|---|
| 1 | market BUY | OK, 0 blocking | real EMA-cross entry, `MARKET`/BUY | entries[0] real condition | `REDUCED_WITH_WARNINGS` |
| 2 | market SELL | OK | real EMA-cross entry, `MARKET`/SELL | entries[0] real condition | `REDUCED_WITH_WARNINGS` |
| 3 | BUY_LIMIT | OK | `OrderSend(OP_BUYLIMIT)` → LIMIT | `executionType:"LIMIT"`, `limitPrice` resolved (literal) | `REDUCED_WITH_WARNINGS` |
| 4 | SELL_LIMIT | OK | `OrderSend(OP_SELLLIMIT)` → LIMIT | same, SELL | `REDUCED_WITH_WARNINGS` |
| 5 | BUY_STOP | OK | `OrderSend(OP_BUYSTOP)` → STOP | `stopPrice` resolved | `REDUCED_WITH_WARNINGS` |
| 6 | SELL_STOP | OK | `OrderSend(OP_SELLSTOP)` → STOP | same, SELL | `REDUCED_WITH_WARNINGS` |
| 7 | `OrderSelect`+`OrderType` | OK | `OrderSelect` recorded as a GATING position query (not a bare accessor) | pyramiding/reversal forced to the conservative branch | **`BLOCKED`** — a real, verified, pre-existing behavior (§ audit) |
| 8 | `OrderSelect`+`OrderModify` guarded by `OrderType()` | OK | `PendingOrderManagementCallSite("OrderModify")`, condition `ORDER_TYPE_FILTER`, target `TICKET` | `pendingOrderManagement.rules[0]`: `MODIFY_STOP`, `semanticFidelity:"EXACT"` | `REDUCED_WITH_WARNINGS` |
| 9 | `OrderSelect`+`OrderDelete` guarded by `OrderType()` | OK | `PendingOrderManagementCallSite("OrderDelete")`, condition `ORDER_TYPE_FILTER` | `CANCEL_PENDING`, `EXACT` | `REDUCED_WITH_WARNINGS` |
| 10 | expiration (`OrderModify` non-zero expiration arg) | OK | `newExpirationExpr` captured | operation `UNKNOWN` (raw datetime not reducible — documented Q0.13/Q1.3 boundary) | `REDUCED_WITH_WARNINGS` |

## MQL5 (11–20)

| # | Fixture | Parsed | Semantic result | IR | Eligibility |
|---|---|---|---|---|---|
| 11 | market order (`CTrade.Buy`) | OK | real EMA-cross entry | `MARKET` | `REDUCED_WITH_WARNINGS` |
| 12 | `CTrade.BuyLimit` | OK | LIMIT | `limitPrice` resolved | `REDUCED_WITH_WARNINGS` |
| 13 | `CTrade.SellLimit` | OK | LIMIT | same, SELL | `REDUCED_WITH_WARNINGS` |
| 14 | `CTrade.BuyStop` | OK | STOP | `stopPrice` resolved | `REDUCED_WITH_WARNINGS` |
| 15 | `CTrade.SellStop` | OK | STOP | same, SELL | `REDUCED_WITH_WARNINGS` |
| 16 | `OrderGetTicket` | OK | recorded in `positionQueries` | no pending-mgmt rule (query only) | `REDUCED_WITH_WARNINGS` |
| 17 | `OrderGetInteger(ORDER_TYPE)` | OK | `CTrade.OrderDelete` detected; condition kind `UNKNOWN` (idiom NOT recognized — real, documented gap) | rule present, `operation.kind:"CANCEL_PENDING"` but `semanticFidelity:"UNKNOWN"` (excluded from execution) | `REDUCED_WITH_WARNINGS` |
| 18 | pending modification (`CTrade.OrderModify` + `OrderType()` guard) | OK | `MODIFY_STOP`, `EXACT` | executable rule | `REDUCED_WITH_WARNINGS` |
| 19 | pending cancellation (`CTrade.OrderDelete` + `OrderType()` guard) | OK | `CANCEL_PENDING`, `EXACT` | executable rule | `REDUCED_WITH_WARNINGS` |
| 20 | expiration (`CTrade.OrderModify` non-zero expiration) | OK | `newExpirationExpr` captured | `UNKNOWN` (same documented boundary as #10) | `REDUCED_WITH_WARNINGS` |

## Negative / adversarial (21–25)

| # | Fixture | Parsed | Semantic result | IR | Eligibility |
|---|---|---|---|---|---|
| 21 | dynamic order type (`OrderSend(...,cmd,...)`, `cmd` a variable) | OK | no resolvable `side` on the order call | `entries[0].direction:"FLAT"`, `UNREPRESENTABLE_CONDITION`, `unsupportedSemantics` BLOCKING | **`BLOCKED`** |
| 22 | unresolved order target (`OrderModify(GetActiveTicket(),...)`) | OK | target resolves `UNKNOWN` | rule present, `target.provable:false` | `REDUCED_WITH_WARNINGS` (the ONE rule is excluded, strategy still reduces) |
| 23 | unresolved external SL/TP (`G01_CalculateSL()`) | OK | `slBinding`/`tpBinding` unresolved | `risk.stopLoss`/`takeProfit` unset, BLOCKING mark | **`BLOCKED`** — a real, verified eligibility rule (exits declaring STOP_LOSS/TAKE_PROFIT require a resolved price) |
| 24 | bid/ask dependency (`OrderSend(...,Ask,...)` as the STOP trigger) | OK | `resolvePriceExprToReference("Ask")` → `{kind:"UNSUPPORTED", reason:"ASK"}` | `stopPrice.kind:"UNSUPPORTED"` | **`BLOCKED`** |
| 25 | ambiguous execution semantics (custom-function-gated entry) | OK | not the one provable simple-condition shape | `UNREPRESENTABLE_CONDITION`, BLOCKING | **`BLOCKED`** |

## Summary

20/25 fixtures reduce (`REDUCED_WITH_WARNINGS` — none reach plain `REDUCED` since every real-world fixture here carries at least one WARNING-severity note, e.g. account-mode assumption or timezone), 5/25 are correctly `BLOCKED`. Every `BLOCKED` outcome traces to a real, named, non-fabricated reason. Zero fixtures produced an `UNKNOWN → EXECUTABLE` conversion (see `docs/Q1.4_SEMANTIC_PRESERVATION.md`).
